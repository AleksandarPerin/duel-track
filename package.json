{
  "name": "dueltrack",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n shared,server,client -c cyan,green,blue \"npm run dev -w @dueltrack/shared\" \"npm run dev -w @dueltrack/server\" \"npm run dev -w @dueltrack/client\"",
    "build": "npm run build -w @dueltrack/shared && npm run build -w @dueltrack/server && npm run build -w @dueltrack/client",
    "test": "npm run test --workspaces --if-present",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:reset": "docker compose down -v && docker compose up -d"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}
